import { X } from "lucide-react";

export const XToggler = () => {
  return (
    <div className="flex items-center justify-center bg-white dark:bg-black">
      <X className="text-gray-600 dark:text-white" />
    </div>
  );
};
